# 数据库配置
# 对于Vercel部署,推荐使用以下PostgreSQL数据库服务之一:
# - Neon (推荐): https://neon.tech/
# - Vercel Postgres: https://vercel.com/docs/storage/vercel-postgres
# - Railway Postgres: https://railway.app/
# - Supabase: https://supabase.com/
#
# Neon连接字符串示例:
# postgresql://user:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require
DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require

# JWT密钥 (用于session加密,请使用随机生成的强密码)
# 可以使用以下命令生成: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this

# AI模型API密钥
# 本应用使用Anthropic Claude API
# 获取API Key: https://console.anthropic.com/
BUILT_IN_FORGE_API_KEY=sk-ant-api03-your-anthropic-api-key

# AI模型API地址 (可选,默认使用 https://api.anthropic.com)
# BUILT_IN_FORGE_API_URL=https://api.anthropic.com

# 应用ID (可选)
VITE_APP_ID=japanese-learning-app

# OAuth配置 (用于用户登录)
# 如果使用Manus平台的OAuth服务
OAUTH_SERVER_URL=https://your-oauth-server.com
OWNER_OPEN_ID=your-owner-open-id

# 服务器端口 (可选,默认3000)
PORT=3000

# Node环境
NODE_ENV=production
